// import { Router } from "express";

// // Themes with top commanders for each

// const themeComRouter = Router();

// themeComRouter.get("/", getAllThemesCom);
// themeComRouter.post("/", addThemeCom);
// themeComRouter.get("/:id", getThemeCom);
// themeComRouter.put("/:id", updateThemeCom);
// themeComRouter.delete("/:id", deleteThemeCom);